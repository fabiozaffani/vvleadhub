# .cursor/rules/ — regras do Cursor (ponteiros + canon gerado)

Regras carregadas pelo Cursor. **`00-canon-vvf.mdc` é GERADO** do vvcore por `vvcore/bin/sync-cursor-rules.sh` — não editar à mão (será sobrescrito). As demais `.mdc` são ponteiros/regras por módulo. Contexto e contrato: [`../README.md`](../README.md).
