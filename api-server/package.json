{
  "name": "@vvf/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:check": "drizzle-kit check"
  },
  "dependencies": {
    "@vvf/contracts": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "drizzle-kit": "^0.28.0"
  },
  "//": "Express 5, pg, pg-boss (fila no schema app), Zod nas bordas entram na Fase 0b — ver docs/tasks/fase-0.md."
}
