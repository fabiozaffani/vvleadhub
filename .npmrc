engine-strict=true
auto-install-peers=true
# Não deixar o pnpm auto-trocar de versão por causa do pin packageManager — usa o pnpm
# presente no ambiente. CI continua determinístico via pnpm/action-setup (instala o 9.15.0).
manage-package-manager-versions=false
