/**
 * Generated by orval v8.18.0 🍺
 * Do not edit manually.
 * Api
 * Contrato HTTP do api-server da VVLEADHUB — fonte de verdade do pipeline
 * de codegen (D-22). Edite este arquivo e rode `pnpm codegen:api` para
 * regenerar `@vvf/api-zod` (validação de borda) e `@vvf/api-client`
 * (hooks React Query).
 *
 * Envelope de erro (specs/engenharia/contrato-http.md): toda resposta de
 * erro segue `ErrorEnvelope`. O valor de `error.code` tem fonte única em
 * `@vvf/contracts` (`errorCodeSchema`) — aqui é `string` por design (D-23),
 * com os códigos válidos listados na descrição do campo.
 *
 * OpenAPI spec version: 0.1.0
 */
export type HealthStatusStatus = typeof HealthStatusStatus[keyof typeof HealthStatusStatus];


export const HealthStatusStatus = {
  ok: 'ok',
} as const;

export interface HealthStatus {
  status: HealthStatusStatus;
  /** Segundos desde o boot do processo. */
  uptime_s?: number;
}

export interface ErrorDetail {
  /** Caminho do campo que falhou (ex.- "phone"). */
  path: string;
  message: string;
}

export type ErrorEnvelopeError = {
  /** Código estável de erro. Fonte única em @vvf/contracts (errorCodeSchema, D-23). Valores- VALIDATION_FAILED, UNAUTHORIZED, FORBIDDEN, NOT_FOUND, CONFLICT, RATE_LIMITED, INTERNAL. O cliente ramifica por este code, não pelo status. */
  code: string;
  /** Mensagem dev-facing (log/Sentry). Nunca é copy de marca. */
  message: string;
  details?: ErrorDetail[];
};

export interface ErrorEnvelope {
  error: ErrorEnvelopeError;
  /** Id de correlação ecoado (observabilidade). */
  request_id?: string;
}

