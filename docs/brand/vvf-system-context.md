# VVF — System Context → fonte única no vvcore

O contexto canônico da empresa (invariantes §2, marca §4, identidade visual §5, produto, métricas, gates) **não vive mais aqui**.

**Fonte única:** `vvcore/plugins/vvcore/context/CONTEXTO-IA.md` (repo `vvcore`).

- **Claude Code:** carregado automaticamente via `@import` no [`CLAUDE.md`](../../CLAUDE.md) — o conteúdo já está na sessão.
- **Editar:** só no vvcore. Nunca aqui.
- **Cursor (quando entrar):** este arquivo passará a ser uma **cópia gerada** do vvcore (`vvcore/bin/sync-context.sh`), para o Composer lê-lo in-repo.

As **Design Guidelines** (sistema visual aplicado, específico do site) continuam aqui: [`vvf-design-guidelines.md`](vvf-design-guidelines.md).
