# docs/ — onde o conhecimento mora

O MD canônico da raiz é [`../AGENTS.md`](../AGENTS.md); [`../CLAUDE.md`](../CLAUDE.md) é só o adapter do Claude Code. O conteúdo vive aqui, na **taxonomia canônica VV** (ARQUITETURA-IA §5). **Comece por [`_index.md`](_index.md)** — o índice load-first é o **único índice enumerável** (módulos · espinha · ordem de leitura); este README é **só o propósito da pasta** (D-5 / §5.1).

**Pipeline:** `discovery → business → specs → system → tasks-drafts → tasks`; `roadmap/` costura em paralelo. Espinha na raiz: `_index.md` · `_lexico.md` · `_decisoes.md`.

> **Marca e doutrina VV-wide não vivem aqui:** `CONTEXTO-IA` (negócio/marca) e `ARQUITETURA-IA` (engenharia) são canônicos no **vvcore** e entram via `.agents/context/` (o Claude Code importa pelo adapter [`../CLAUDE.md`](../CLAUDE.md)). Referencie como `CONTEXTO-IA §x`.
