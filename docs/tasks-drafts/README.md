# tasks-drafts/ — work-orders sugeridas (pré-promoção)

WO **rascunhadas** que ainda não foram autorizadas a virar [`../tasks/`](../tasks/). Estágio anterior no pipeline canônico VV:

`discovery → business → specs → system → tasks-drafts → tasks`

Uma WO nasce aqui (escopo proposto, allowlist tentativa, critério de aceite em discussão). Com o **aval do fundador**, é **promovida** para `tasks/` — sai daqui e passa a valer. Mesma anatomia de WO do `tasks/` (escopo, fronteira/allowlist, critério de aceite); ver o padrão em [`../tasks/`](../tasks/).

> Promover = mover o arquivo de `tasks-drafts/` para `tasks/` e marcar `status: pending`. Enquanto aqui, a WO não é executada.
