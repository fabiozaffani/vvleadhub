# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.astro/
.next/
.output/
.vercel/
.turbo/

# env / secrets (NUNCA commitar — só .env.example)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/
.lighthouseci/

# editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Payload / generated runtime
admin/.payload/

# Resíduo de ambiente local / legado de tooling (não versionar)
.local/
.cache/
.upm/
.replit
replit.nix

# Proxy de dev legado (Replit single-port) — produção usa subdomínios no Cloudflare (D-18)
proxy.mjs

# Link por máquina para .agents/skills (junction no Windows, symlink no macOS/Linux; recriado pelo setup-links.sh do vvcore).
/.claude/skills/

# Link por máquina para .agents/context -> vvcore/plugins/vvcore/context (junction no Windows, symlink no macOS/Linux; canon via @import).
# Recriado pelo setup-links.sh do vvcore (link_repo_context — WO-08). Versionar copiaria os arquivos
# do vvcore e furaria a fonte única (D-16/D-18).
/.agents/context/

# Config per-maquina do Claude (autoMemoryDirectory absoluto + permissions locais).
# Defesa em profundidade: ja coberto pelo gitignore global do usuario, mas garantir aqui
# evita vazar caminho local num clone/CI sem o global.
/.claude/settings.local.json
