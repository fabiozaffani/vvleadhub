# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.astro/
.next/
.output/
.vercel/
.turbo/

# env / secrets (NUNCA commitar — só .env.example)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/
.lighthouseci/

# editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Payload / generated runtime
admin/.payload/

# Resíduo de ambiente local / legado de tooling (não versionar)
.local/
.cache/
.upm/
.replit
replit.nix

# Proxy de dev legado (Replit single-port) — produção usa subdomínios no Cloudflare (D-18)
proxy.mjs

# Junction per-maquina para o .agents/skills (recriado pelo setup-links.sh do vvcore).
/.claude/skills/

# Junction per-maquina para o .agents/context -> vvcore/plugins/vvcore/context (canon via @import).
# Recriado pelo setup-links.sh do vvcore (link_repo_context — WO-08). Versionar copiaria os 3
# arquivos do vvcore (git nao entende junction, copia incondicionalmente) e furaria a fonte unica (D-16/D-18).
/.agents/context/
